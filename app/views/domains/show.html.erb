<h1><%= @domain.domain %></h1>

<p id="notice"><%= notice %></p>

<ul>
  <li>Mailbox: <%= @domain.rel_mailboxes.count %> / <%= @domain.mailboxes %></li>
  <li>Alias: <%= @domain.pure_aliases.count %> / <%= @domain.aliases %></li>
  <li>Quota (MB): <%= @domain.maxquota %></li>
  <li>Active: <%= @domain.active %></li>
  <li>Modified: <%= @domain.modified %></li>
</ul>

<h3>Mailbox</h3>
<table>
  <% @domain.rel_mailboxes.each do |m| %>
    <tr>
      <td><%= link_to m.username, edit_mailbox_path(m) %></td>
      <td><%= link_to "alias", edit_alias_path(m.alias) %></td>
      <td><%= m.name %></td>
      <td><%= m.alias.goto %></td>
      <td><%= m.quota %></td>
      <td><%= m.active %></td>
      <td><%= m.modified %></td>
    </tr>
  <% end %>
</table>

<h3>Alias</h3>
<table>
  <% @domain.pure_aliases.each do |a| %>
    <tr>
      <td><%= link_to a.address, edit_alias_path(a) %></td>
      <td><%= a.goto %></td>
      <td><%= a.active %></td>
      <td><%= a.modified %></td>
    </tr>
  <% end %>
</table>